---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webhook-receiver
  namespace: flux-system
  labels:
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: flux-webhook
    app.kubernetes.io/managed-by: Flux
    app.kubernetes.io/name: flux-webhook
    app.kubernetes.io/part-of: temporal
    app.kubernetes.io/version: 1.19.1
spec:
  ingressClassName: nginx
  rules:
    - host: flux---webhook.shanginn.io
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: webhook-receiver
                port:
                  number: 80
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: webapp
  namespace: flux-system
spec:
  interval: 60m
  url: https://github.com/shanginn/infra-flux
  ref:
    branch: main
---
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: webapp
  namespace: flux-system
spec:
  type: github
  events:
    - "ping"
    - "push"
  secretRef:
    name: webhook-token
  resources:
    - kind: GitRepository
      name: webapp