apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mail-system

resources:
  - ../../../components/mail-server/base
  - ../../../mail
  - admin-allowlist.enc.yaml
  - service-public-smtp.yaml
  - networkpolicy-public-smtp.yaml

# Recovery bootstrap completed. The component and its encrypted credential
# must remain absent during normal operation.

# The full public L4/user component is intentionally absent. Only inbound
# SMTP/25 is published above; submission, IMAPS and the user UI stay private.
# See: ../../../components/mail-server/cutover/kustomization.yaml
