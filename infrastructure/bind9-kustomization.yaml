apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: bind9
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/rubase/bind9
  prune: true
  dependsOn:
    - name: infrastructure
  sourceRef:
    kind: GitRepository
    name: flux-system
