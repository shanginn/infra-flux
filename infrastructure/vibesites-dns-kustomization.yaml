apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vibesites-dns
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/rubase/vibesites-dns
  prune: true
  dependsOn:
    - name: infrastructure
  sourceRef:
    kind: GitRepository
    name: flux-system
